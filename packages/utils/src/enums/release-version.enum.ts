/**
 * 发布的版本类型
 */
export enum ReleaseVersionTypeEnum {
  MAJOR = 'major',
  PRE_MAJOR = 'premajor',
  MINOR = 'minor',
  PRE_MINOR = 'preminor',
  PATCH = 'patch',
  PRE_PATCH = 'prepatch',
  PRE_RELEASE = 'prerelease',
  NEXT = 'next',
}
